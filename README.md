# GrantOps Public

Public GitHub Pages view of the private GrantOps operating database.

The public site is intentionally separated from the private source repository. Data is exported from `Obefree/GrantOps` into a public-safe JSON snapshot and rendered here.
